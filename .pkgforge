name 'sed'
org 'amylum'

licenses 'COPYING'

source(
  type: 'tar',
  url: 'https://ftp.gnu.org/gnu/sed/sed-4.3.tar.gz'
)

configure_flags(
  prefix: '/usr'
)

harden

build do
  configure
  make
  install
end

test do
  # TODO: add tests
end

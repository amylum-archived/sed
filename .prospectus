item do
  expected do
    url_xpath
    url 'http://git.savannah.gnu.org/cgit/sed.git/refs/'
    xpath '//*[@id="cgit"]/div/table/tr[6]/td[1]/a'
    regex /^v([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
